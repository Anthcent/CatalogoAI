import { db } from "@/lib/db";
import { createTemplateAction } from "@/modules/templates/actions";

export default async function NewTemplatePage() {
  const [businesses, types] = await Promise.all([db.business.findMany({ where: { active: true } }), db.itemType.findMany({ orderBy: { name: "asc" } })]);
  return <main className="page"><span className="eyebrow">Estructura reutilizable</span><h1>Crear una plantilla</h1><form action={createTemplateAction} className="card form-stack" style={{maxWidth:750}}><label>Nombre<input className="field" name="name" required autoFocus/></label><label>Descripción<textarea className="field" name="description" rows={3}/></label><div className="relation-form"><label>Empresa recomendada<select className="field" name="businessId"><option value="">Cualquier empresa</option>{businesses.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Tipo recomendado<select className="field" name="typeId"><option value="">Cualquier tipo</option>{types.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label></div><label>Secciones iniciales<textarea className="field" name="structure" rows={9} placeholder={"Objetivo\nMateriales\nPasos\nResultado\nNotas"}/><small className="subtle">Una sección por línea. La estructura se copia al crear un elemento.</small></label><button className="btn primary">Guardar plantilla</button></form></main>;
}
